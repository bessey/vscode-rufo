{
  "name": "vscode-rufo",
  "displayName": "rufo",
  "description": "Ruby formatter",
  "version": "0.0.1",
  "publisher": "mbessey",
  "engines": {
    "vscode": "^1.13.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:extension.sayHello"
  ],
  "main": "./out/src/extension",
  "contributes": {
    "commands": [
      {
        "command": "extension.formatRuby",
        "title": "Format ruby file"
      }
    ],
    "keybindings": [
      {
        "command": "extension.formatRuby",
        "key": "ctrl+shift+I",
        "mac": "cmd+shift+I",
        "when": "editorTextFocus"
      }
    ],
    "configuration": {
      "type": "object",
      "title": "Rufo configuration",
      "properties": {
        "rufo.formatOnSave": {
          "type": "boolean",
          "default": false,
          "description": "Wether to format on every save or not."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "tsc -p ./",
    "compile": "tsc -watch -p ./",
    "postinstall": "node ./node_modules/vscode/bin/install",
    "test": "node ./node_modules/vscode/bin/test"
  },
  "devDependencies": {
    "@types/mocha": "^2.2.32",
    "@types/node": "^6.0.40",
    "mocha": "^2.3.3",
    "tslint": "^5.5.0",
    "typescript": "^2.0.3",
    "vscode": "^1.0.0"
  }
}
